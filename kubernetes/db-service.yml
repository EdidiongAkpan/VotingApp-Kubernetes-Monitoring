apiVersion: v1
kind: Service
metadata:
  name: db-service
  labels:
    app: db-app
spec:
  type: ClusterIP
  selector:
    app: db-app
  ports:
    - name: mysql 
      port: 3306
      targetPort: 3306
